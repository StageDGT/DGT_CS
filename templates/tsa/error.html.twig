{% extends 'base.html.twig' %}

{% block body %}
    <main>
        <table width="100%" height="100%">
            <tr width="100%" height="100%">
                {% include 'menu_deroulant.html.twig' %}
                <td width="100%" class="text-center" onmouseover="FermerMenu()">
                    <!-- Contenu de la page -->
                    <div class="text-align center">
                    {% if app.user.role == 1 %}
                        <h1><strong>Erreur, impossible de se connecter. Verifiez vos paramètres de connexions <a href="{{ path('modifierParametres') }}">ici</a>.</strong></h1>
                    {% endif %}
                    {% if app.user.role == 2 %}
                        <h1><strong>Erreur, impossible de se connecter. Veuillez contacter votre administrateur.</strong></h1>
                    {% endif %}
                    </div>
                </td>
            </tr>
        </table>
    </main>
{% endblock %}